<!-- Navbar -->
<nav class="navbar navbar-expand-lg bg-light">
  <div class="container">
    <a href="/" class="navbar-brand">
      <img
        src="/img/user-profile.png"
        alt=""
        class="navbar-logo"
        width="50"
        height="50"
      />
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage "home") }}
           active fw-medium
          {{/if}}
          " aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage "project") }}
           active fw-medium
          {{/if}}
          " href="/projects">Projects</a>
        </li>
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage "blogs") }}
           active fw-medium
          {{/if}}
          " href="/blogs">Blogs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage "testimonials") }}
           active fw-medium
          {{/if}}
          " href="/testimonials">Testimonials</a>
        </li>
      </ul>
      <div class="d-flex flex-column flex-lg-row gap-3 justify-content-lg-center align-items-lg-center">
        {{#if user}}
        <span class="me-3">Selamat datang, {{user.name}}</span>
        <a href="/logout" class="btn btn-secondary px-3 py-2 me-0 me-lg-3 rounded-5" type="submit">Logout</a>
        {{/if}}
        <a href="/contact" class="btn px-3 py-2 btn-dark rounded-5">
          Contact Us
        </a>
        {{#if user}}
        {{else}}
        <a href="/login" class="btn px-3 py-2 btn-dark rounded-5">
          Login
        </a>
        <a href="/register" class="btn px-3 py-2 btn-dark rounded-5">
          Register
        </a>
        {{/if}}
      </div>
    </div>
  </div>
</nav>